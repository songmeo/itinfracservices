[Unit]
Description=Mysqld Exporter
Wants=network.target
After=network.target

[Service]
Environment="DATA_SOURCE_NAME=kat:kat@(server:3306)"
Type=simple
ExecStart=/opt/mysqld_exporter/mysqld_exporter
Restart=always

[Install]
WantedBy=multi-user.target
